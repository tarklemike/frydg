  <div class="search-container">
    <%= form_with url: recipes_path, method: :get, class: "searchbar d-flex" do |form| %>
      <%= text_field_tag :query,
            params[:query],
            class: "form-control",
            placeholder: "Search"
        %>
     <%= form.submit "GO", class: "button_default" %>
    <% end %>
  </div>
     <%# <a><button type="submit" class="button_default"> GO </button></a> %>


<style>
  .searchbar {
    border-radius: 20px;
    min-width: 360px;
    height: 60px;
  }
  .search-container {
  display: flex;
  flex-direction: row;
  align-items: center;
  justify-content: space-between;
  }
</style>
