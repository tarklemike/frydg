
<%# this page is search results and links to the recipe card scss %>
<div class="container">
  <div class="wrapper d-flex">
    <div class="row">
      <div class="side_bar col-2">
        <div class="d-flex flex-column">
        <div class="addcomponenet">
          <p>add</p>
          <p>search bar</p>
        </div>
          <p class="retractablecontainer">Allergens</p>
          <%# TODO - ADD ALLERGENS %>
            <p></p>
          <p class="retractablecontainer">Cuisine</p>
          <%# TODO - ADD CUISINES %>
        </div>
      </div>
        <div class="col-10">
          <h1 class="pagetitle">Sustainably Scrumptious Recipes</h1>
          <div class="card-grid">
                <% @recipes.each do |recipe| %>
                  <%= link_to recipe_path(recipe) do %>
                  <%= render "shared/cardrecipe", recipe: recipe %>
                  <% end %>
                <% end %>
          </div>

      <a href="#"><button class="button_default"><strong>Back To Top</strong></button></a>
    </div>
    </div>
  </div>
</div>
